# -------------------------------------------------------------
# Project-specific ignore rules for ~/bin/python
# -------------------------------------------------------------
# Shared defaults (Python bytecode, venvs, node_modules, editor
# artefacts, etc.) now live in ~/.gitignore_global. This file only
# lists paths that are unique to the chat-processing toolkit.
# -------------------------------------------------------------

# Scratch workspace for in-flight conversions and debugging.
tmp/
